##### 04
最后仍然是训练失败了，过了一定阈值就会loss暴增，但是val的也没有变好
angle_loss，rot_loss，seqs_loss，torsion_loss，trans_loss暴增，可能还是处理某些边界条件出的问题，或者因为没有dropout

pepflow还是要测试完

ppt思路，先讲研究现状，即两篇文章，再讲研究计划，分数据，模型，评估三个部分
对于数据
沿用pepmimc的思路。基本数据集加上片段数据集，但片段数据集的抽取方式未必完全合理
另外暂时不考虑ppi，目前的限制受体长度，但是显然bindcraft的成功是基于af2-multimer，整个pdb的，里面有大量多链
但我相信在整个数据集上表现好的，一定也能在多肽上表现好
对于模型，我意思就是用于benchmark的模型，使用pepmimc，rapidock，rfdiffusion，bindcraft进行benchmark
对于评估
分两类，一类是基于模版的指标，就是相似度，包括简单可计算的rmsd，以及复杂但鲁棒iptm等
一类是基于物理的指标，例如结合能，我仍然希望最终在结合能上评估，尽可能贴近现实应用


目前进展，数据方面，还没有做，设计新数据集可能太耗时了

模型方面，需要复现，重新训练的，pepmimc，rapidock，pepflow，pepflow基于af2模型骨架，是一个很强的baseline。不需要重新训练的，如rfdiffusion，bindcraft，需要跑通其流程

##### 07
pepflow自己跑失败了，应该是重建几何有误，但是现在只能放下了


重点进一步放在UAE上面

La-Proteina
全原子扩散的典型

##### 09
原本的UAE完全不可用，可以放弃，希望能重构，La-Proteina是成功版本

pepflow训练不成功，应该还是流程有bug
训练和推理很可能都有bug
训练是loss陡增，
目前最直接怀疑的点是loss权重，但背后原因也许是有不稳定的loss计算方式，但是这个loss计算很复杂，我没有时间从头梳理，只能放在这里
推理
从结果来看，首先残基原子坐标都是重合的，错误，其次主链位置都是错的，似乎只是在高斯分布周围
因此很可能训练和推理流程都有误，我太急功近利，相信gpt了

对于UAE，我认为应该模仿simplefold

##### 12
还是没怎么想好要怎么做
但本质原因是我还没有完全理解算法本质
从pepglad的amegcl说起
首先，用coord2radial
计算一个edge，C,C的全原子欧式距离矩阵，
